import 'package:flutter/material.dart';

void errorLogger(String error) {
  debugPrint("=====================================");
  debugPrint(error);
  debugPrint("=====================================");
}
